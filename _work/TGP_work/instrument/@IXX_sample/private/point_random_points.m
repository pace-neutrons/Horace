function x = point_random_points (ps, np)
% Return an array of random points uniformly distributed in a point sample
%
%   >> x = cuboid_random_points(ps, np)
%
% Input:
% -------
%   ps      Arguments for point sample. Empty array [].
%   np      Number of points
%
% Output:
% -------
%   x       Array of coordinates, size [3,np]

x = zeros(3,np);
